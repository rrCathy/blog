# 博客写作手册

从第一篇文章《从正四面体到 A₄，再到群论和拓扑图论》(`src/content/blog/260909_tetrahedron-a4.mdx`) 的全过程复盘出来的。那篇 317 行、8 个折叠块、9 张活图，从初稿到定稿改了 6 轮。这里记的是踩过的坑和验证过管用的做法，不是写作理论。

---

## 一、发布流程

**一篇新文章就是加一个文件。** 在 `src/content/blog/` 下新建 `YYMMDD_短slug.mdx`，写完 commit + push，CI 约两分钟自动上线。不用碰别的地方。

命名格式 `YYMMDD_短slug.mdx`，例如 `260909_tetrahedron-a4.mdx`。文件名就是 URL，保持短、用连字符分词、别用中文。模板在仓库根目录 `post-template.mdx`，复制改 frontmatter 即可。

frontmatter 字段（schema 见 `src/content.config.ts`）：

| 字段 | 必填 | 说明 |
|---|---|---|
| `title` | 是 | 显示在列表与浏览器标签 |
| `description` | 否 | 列表页与 RSS 的一句话摘要 |
| `pubDate` | 是 | 发布日期 |
| `updateDate` | 否 | 有实质修订时补上，页脚会显示「最后更新」 |
| `tags` | 否 | 数组，展示侧自动聚合 |
| `draft` | 否 | `true` 则不进列表与 RSS |

**MDX 的三个坑**（都实际踩过）：

1. 注释用 `{/* */}`，不能用 `<!-- -->`。
2. JSX 属性里的引号用 ASCII 直引号 `"`，中文引号会报错。
3. 表格必须靠 `remarkGfm`，`astro.config.mjs` 的 `mdx()` 和 `markdown` 两块都要显式挂上（已配好，新插件别漏这两处）。公式走 `remark-math` + `rehype-katex`。

**写完必须验证两件事**，不能只看构建过没过：

```bash
npm run build          # 编译
# 去产出里 grep 关键文案，确认真的落盘了
grep -o "新写的某句话" dist/<slug>/index.html
```

改稿时尤其要 grep。同一轮里连发多个编辑有撞车风险——有一次改 caption 报了成功但没落盘，靠 grep 产出才发现。**改完一个文件，先确认再改下一个。**

---

## 二、内容架构：先解决受众，再谈别的

第一篇最大的结构问题是**受众断层**：前半篇从「什么是群」讲起，后半篇直接上 Sylow 定理、半直积、商群。同一个读者不可能同时是这两种人。这是科普长文流失读者风险最高的地方，通常出现在「初等内容讲完、开始上强度」的那个接缝。

验证有效的做法是**分层**：

- **正文是一条只给初学者的主线**，只用最少的前置。开头放一段「怎么读这篇」，明确告诉读者折叠块可以跳过、跳过不影响主线。
- **严格论证全部收进折叠块** `<Tip label="…">…</Tip>`（`src/components/Tip.astro`，纯静态 `<details>`，无 JS）。计数、推导、证明、构造都放这里，想较真的读者自己点开。

判断某段该不该下沉到 Tip，问一句：**不懂这段，主线还能不能读下去？** 能，就下沉。

第一篇最终 8 个 Tip，覆盖的正是「深水区」：12 个旋转的计数证明、奇偶性严格说明、Sylow 计数与共轭、半直积定义、Lagrange 反例的三行证明、覆盖空间严格版、A₄ 无 6 阶子群。

同理，Sylow 那节正文措辞要软化的地方就软化——正文只需要读者「知道有这回事」，证明交给 Tip。

---

## 三、数学文的两类硬伤

**第一类，把常见误记当成事实。** 第一篇原稿写「缺的那 12 种是形如 $(12)$ 这种只交换两个标签的操作」。这是错的：$S_4$ 的 12 个奇置换里只有 6 个是对换，另外 6 个是 $(1234)$ 这类 4-循环。「奇 = 对换」是读者最容易犯的错，文章一旦替他们确认，反而强化误记。凡是要写「显然是」「无非是」的地方，字面算一遍。

**第二类，隐性预设。** 把「凯莱图长得像截角四面体」当成群的固有属性写，隐去了它其实**依赖生成元集的选取**。同一个 $A_4$ 换一组生成元，图的度数、边数、平面性全变。第一篇补了具体反例：$A_4$ 用 $a=(12)(34),b=(234)$ 得到 3-正则/18 边；换成两个 3-循环 $b=(234),c=(134)$ 立刻变 4-正则/24 边。

写定理型结论时留个心：这句话对**什么**成立？参数是固定的还是可选的？把条件写进表格表头和正文，别让它悬着。

**核查手段是跑引擎，不是凭记忆。** 第一篇验证乘法表结构时，直接写脚本调 `createGroupFromSymbol('A4')` 打印元素顺序，发现是 id 字典序，据此确认了「左上 3×3 是 $C_3$、左上 4×4 不是群」（原文写错了）。这类脚本临时放在仓库里跑完就删。

---

## 四、可视化：优先活图

有引擎就直接上活图，别手搓 SVG。第一篇初稿画了几张静态 SVG，后来全换成 `GroupScene`。手搓的图既不好看也不可交互，费力不讨好。

**视图选择：**

| 想讲什么 | `view` | 备注 |
|---|---|---|
| 元素构成、摊开看 | `set` | |
| 元素阶、循环子群 | `cycle` | |
| 生成元与连边 | `cayley` | 2D |
| 同上，要立体感 | `cayley3d` | 多面体布局，可拖拽旋转 |
| 运算律、子群、中心 | `table` | 可点行列看乘积 |
| 几何体的对称作用 | `symmetry` | 见下 |

对称性是第一篇的主力（用了 5 次）。讲旋转对几何体的作用时，用 `symmetry` 视图加**元素操作台**让读者自己选元素看效果，比单张静态图强得多：`picker` 开操作台，`pickerOrders={[3]}` 只放出 3 阶元，`actionElement="(234)"` 指定默认演示的元素，`autoDemo` 自动循环重播（`showAction` 期间相机旋转会被禁，靠这个让读者能重看）。

**几条视觉约定：**

- **用颜色图例代替编号。** 不要给顶点标 1/2/3/4 让读者对照，直接说「红轴 = 旋转轴，黄球 = 固定顶点，青球 = 棱中点」。
- **不常驻标签，靠 hover 读信息。** 这是本项目一贯的语义，`showLabels={false}` 配合悬停气泡。
- **subgroup + members 出子群表。** 讲 $V_4$ 时别在 $A_4$ 大表里绕，直接切出子群：`<GroupScene symbol="A4" subgroup="V₄" members="(12)(34),(13)(24),(14)(23)" view="table" cellSize={72} />`。`members` 是逗号分隔的元素**记号**，恒等元自动补入；`cellSize` 调大能缓解长记号表头重叠。

**引擎便利层（2.1.2 起）**：引擎自带了外部嵌入所需的一层，壳已改用它，改壳前先知道这几条：

- 元素引用类 props（`actionElement`、`actions[].elementId`）现在接受 **label / id / 循环记号**任一写法，内部经 `core.resolveElement` 解析，写错会 `console.warn`（不再静默）。2.1 之前必须传 id（逗号串 `"1,3,4,2"`），写错会导致动画不播、相机还被锁死，难查。
- 2D 视图的状态 / 平移缩放 / hover 交给 `useSceneState`（`hostProps` + `sceneProps` + `hoverBubble`）；元素阶用 `core.elementOrder(group, el)`（2.1 起是 group-first 公共入口）。这两样以前都要宿主手写。
- 2D Scene 现在都有 `theme` prop；本项目仍在 `.gv-scene` 外层挂 `data-theme`，因为壳自己的操作台和说明栏也吃这套变量。
- 乘法表是静态表格，壳里对 `view === 'table'` 关掉了平移缩放（`enablePan`/`enableZoom: false`），否则读者能把表拖出画布；图形视图保留平移缩放。
- 完整 props 清单见 `src/components/GroupScene.tsx` 的 `GroupSceneProps`。

完整 props 清单见 `src/components/GroupScene.tsx` 顶部的 `GroupSceneProps`。改壳时以那份为准，本文档只记了常用的和容易踩的。

---

## 五、去 AI 味

第一篇用 `humanizer` skill 过了一遍。扫描下来，真正的 AI 味集中在这几项，写的时候就该盯住：

| 指标 | 初稿 | 定稿 |
|---|---|---|
| 破折号 `——` | 32 | 0 |
| 冒号 `：` | 80 | 0 |
| 双引号 `""` | 55 对 | 7 |
| 加粗片段 | 78 | 26 |
| `恰好`/`正是`/`正好` | 12/10/4 | 1/2/3 |

外加消灭：「不是A而是B」三毒、`值得注意`/`综上所述`/`首先`这类套路词、每节一个反问收尾、「答案在下一节」「只是一个开始」「点到为止」这种升华腔。

**一处必须说明的分级原则。** 那个 skill 把冒号、破折号、双引号列为一律禁用，它是按口语文案设计的。**硬套到数学科普会伤内容**：用冒号引出一张公式或一张表是标准排版，`<Tip label="…">` 里的引号是组件属性、改了会和界面文案对不上。

所以标点规则按语域降级执行：清掉「讲义腔」的那部分（"答案是…："、"先看…："），数学和结构上必需的保留。禁用词、三毒、堆叠副词、复读、套路开场与升华、加粗密度这些实质项全量照做。

**结尾不要升华。** 初稿结尾是「其实只用了两个动作」这种调子，改成平铺的两句话收束。

**第三篇修订后追加（2026-09-12）——两种前两篇没踩过的新坑：**

- **故作短句金句。**「写下来。」「命名借别人的，东西是自己的。」这类两三字断言独立成段，第一人称叙事里读着像格言集。检测办法：把句子单独拿出来，如果它除了「显得深刻」不承担任何信息量，删，或者改写成承担具体信息的话。
- **没有上下文的承接句。**「先纠一个命名」「这个定理带来的第一件礼物」——默认前文出现过「命名之争」「礼物清单」，但前文根本没有。每处「纠」「补」「回到」都必须能指出前文锚点，指不出就重写该段，把「为什么要有这个说法」本身交代出来。

---

## 六、审稿流程

找真人读，越挑剔越好。第一篇靠朋友的一篇锐评找出三个问题（硬伤、受众断层、隐藏条件），这三个都是自己写的时候看不出来的。

**修错优先级：事实硬伤 > 结构（受众）> 隐藏条件 > 文字打磨。** 硬伤必须改，不是措辞偏好问题。

改完做三项实测，别只信构建：

1. `npm run build` 通过；
2. 去产出 HTML 里 grep 新旧文案（旧声明 0 处、新文案 1 处）；
3. 真机截图。用本地 chromium（`C:/Users/bluej/AppData/Local/ms-playwright/chromium-1234`）+ `playwright-core`，移动端 390 视口看有没有横向溢出、左右边距是否一致（应为 `--gutter`，窄屏 1.15rem ≈ 18px）、展示公式和折叠块是否正常。

顺带清理无 JS 环境下的 LaTeX 残迹，`$4\times2=8$` 这类内联公式在某些场景会漏渲染，能写纯文本就写纯文本。

---

## 七、给下一篇的备忘

1. **深挖一个点，别急着拓宽。** 拓扑图论那节已经点到为止了。下一篇与其继续往外铺，不如把这次被迫隐去的假设变成主题，单独写「凯莱图与生成元的选取」，正好接住已经做出来的可视化引擎。
2. **引擎侧待办**（等引擎那边改完再回看）：元素引用类 props 能接受 label 或 id 并内部解析；`TableView` 已支持 `subsets` 做子群着色但壳没透出；2D 视图缺 `theme` prop。
3. **提交前**：确认 `updateDate` 有值，页脚「最后更新」才会显示。

## 八、第二篇之后补记

第二篇《凯莱图：从手绘到代码，再到引擎》写完后追加。

**引擎 bug，已修（v2.1.4，commit `dcd792d`）：** 原 `CayleyView.tsx` 算圆形布局半径时只看宽度（`Math.min(viewBoxSize.width * 0.3, 180 + n * 10)`），没管高度。**只影响 `circular` 形状**（S₃、循环群 Cₙ，以及回退到圆形的群）。容器宽扁（如 900×360）时半径超过 `height/2`，节点被推出画布：S₃ 的半径恒为 `180+6*10=240`，所以 `height` 得 >480 才安全；`A₄` 走 `projection3D`，**不受影响**。

修法正是当时预判的：新增共享纯函数 `circleLayoutRadius(width, height, n, nodeRadius, opts?)`（`src/core/algebra/layouts/shared.ts`），半径取 `min(width×0.3, halfW, halfH, 180+n×10)`，五个调用点（CayleyView / FloatingViewWindow / GroupCanvas / SylowView / CycleView）统一改走它，测试 +7，publish-smoke 加了永久门禁。

**验证做法（可复用）：** blog 侧装的是 npm 上的 `@groupviz/core` / `@groupviz/react`，引擎改完要先 `npm i @groupviz/core@<ver> @groupviz/react@<ver>`（注意 npm 可能报 `up to date` 但仍装了新版本，务必 `grep version node_modules/@groupviz/core/package.json` 复核）。然后 build + preview + Playwright 真机量节点外沿。

量节点别去抓 `<circle>`——`CayleyView` 的节点圆坐标在父级 `<g transform="translate(x,y)">` 上，直接抓 circle 的 `cx/cy` 全是 0，会得到假的「全部在原点」。正确姿势：`g[style*="cursor"]` 里的 `translate` 配 circle 的 `r`。3D 视图（`cayley3d`）用的是 WebGL canvas 不是 SVG，量不到 `astro-island svg`，要单独按 canvas 检查。

**实测存档（900×360 视口，画布 750×H，nodeRadius=28）：**

| 场景 | 修复前 yRange | 修复后 yRange | 结论 |
|---|---|---|---|
| S₃ circular h=360 | `[-88, 448]`（溢出 ±88） | `[16.0, 344.0]` | 修复 |
| C₆ circular h=360 | 同上 | `[16.0, 344.0]` | 修复 |
| S₃ circular h=244（极端） | 溢出 | `[16.0, 224.0]` | 修复 |
| A₄ projection3D h=360 | `[52.0, 308.0]` | `[52.0, 308.0]` | 未误伤 |

修复后文章里那两张 S₃ circular 的 `height` 已从绕 bug 的 `560` 收到 `440`。回归页留在 `src/content/blog/zzbug.mdx`（`draft: true`，不进列表和 RSS），含 5 格含极端宽扁的对照组。

**移动端代码块贴边（已修）：** `global.css` 的 `@media (max-width: 640px)` 里原本有一条 `.article-body pre { margin-inline: calc(-1 * var(--gutter)); border-radius: 0 }`，让代码块全宽出血到屏幕边缘。结果是 390 视口下代码块从 `18.4px` 的正文边距被拉到 `0 → 390`，左右零空隙、圆角归零，和正文左对齐线错位。**出血是个刻意的设计，但在有侧边距的正文里反而破坏了对齐节奏**，已删掉，代码块现在与正文同宽同边距（四视口实测 `aligned: true`：390 / 360 / 768 / 1280）。

顺带暴露的第二处：Shiki 只给 `pre` 设了背景色，**没有内边距**（内联 style 只有 `background-color` / `color` / `overflow-x` / `white-space`），之前被负 margin 视觉掩盖。基础样式里补了 `padding: 0.95em 1.05em` + `margin: 1.6em 0`。以后动代码块样式记得这两处是一套。

**改样式后的验证口径：** 别只看 390 一个视口。用 390 / 360 / 768 / 1280 四挡量 `pre` 与 `.article-body` 的左右边界差（应 < 0.6px），再截图肉眼看。活图（GroupScene）在 390 下边距和圆角都正常，操作台的标题截断（`ellipsis`）是引擎既有行为，不是 bug。

**壳已补的能力（第二篇用上了）：** `GroupScene` 现在支持 `actions`（逗号分隔的元素引用，如 `actions="(12),(23)"`）和 `multiplyType`。引擎侧 `CayleyView`/`Cayley3DScene` 一直有这两个 prop，是壳之前没透出。`actions` 缺省不传时行为与从前完全一致。

**各群的 2D 默认形状不一样，配图前先查：** A₄/A₅/Sₙ 走 `projection3D`（多面体等轴投影，节点会重叠），S₃ 和循环群走 `circular`。判断用 `getDefaultShape2D(group)`。`projection3D` 的坐标按视口缩放，高度给大图就大；`circular` 半径与视口无关，高度不够就裁。

**这条很值得记住：写「凯莱图依赖生成元」这类主题时，活图必须能换生成元，否则论点只能靠文字撑。** 换生成元的对比（节点位置不动、边全变）在 3D 视图下视觉效果极强，比 2D 好，值得优先用 `cayley3d`。

**核查手段固定下来：** 引擎能在 Node 里直接 `require` 跑（`node_modules/@groupviz/core/index.js`），写临时脚本调 `createGroupFromSymbol` + `computeCayleyActionEdges` 验证边数、度数、邻接，比手算可靠。脚本含模板字符串时用 Write 工具写文件，别用 heredoc（`${}` 会被 shell 吞）。

## 九、外部锐评核实清单（2026-09-11，全部处理完毕）

两份外部 AI 锐评逐条核实后的结论。**进度**：文章层 3–7 已随润色处理；**网站层 1、8–12 已全部修复并真机验证**（09-11 下午，详见下）；13 观察项按设计保留；误报四条不修。

### 真 bug（可直接修）

1. **Issue 链接插值失效**：`src/pages/[...slug].astro:78` 写的是 `href="...?title=关于「{post.data.title}」"`——JSX/Astro 规则里**双引号属性值是纯字符串，花括号不插值**，dist 实测两篇文章的链接都是字面量 `{post.data.title}`，点开全指向同一个占位标题。修法：改成 `href={模板字符串}`，顺手 `encodeURIComponent`。
2. **「二十来行」vs「20 行」**：文章 194 行与 363 行对同一段代码的行数说法不一致。

### 文章内容（第二篇，锐评 1）

3. **作者身份含糊**：「我扒的是 GroupViz 的引擎」——引擎就是自己写的，应大方承认，别营造客观分析的口吻。
4. **A₄ 缺直观锚点**：「正四面体的旋转群」一句带过 + 指向第一篇，没读过第一篇的外行接不住。补一两句直观描述。
5. **4.2/4.3 源码摘录症**：17 行瀑布链和 `faceIds` 数组是噪音，洞察只有两句（判断顺序有讲究；只认群是谁不认生成元是谁）。可精简到 5 行内或伪代码。
6. **4.4「崩」的措辞过重**：实际是「位置对、边不对、图还能看」。要么改措辞为「名不副实」，要么把第二节已有的 (234),(134) 活图在此处再强调一次。
7. 低优先级：括注偏多（「引擎内部叫 projection3D」类）、部分段落超 3 行。

### 产品功能（锐评 2，都要写代码，单独排期）

8. **长文无 TOC**：两篇都 12k+ 字带 KaTeX。可做右侧浮动目录（IntersectionObserver 高亮）。
9. **无上一篇/下一篇导航**；首页按日期倒序，新读者先看到进阶篇。可加系列导航或置顶入门篇。
10. **代码块无复制按钮**。
11. **首页标签是装饰**：`index.astro:32` 是 `<span>#{t}</span>` 不可点击，但容器挂着 `aria-label="文章标签"`，有误导。要么真做过滤，要么改叫「主题词」。
12. **页脚「（在线时间 10:00–24:00）」**（`[...slug].astro:70`）：QQ 签名档气质，删或移 /about。
13. 观察项：KaTeX 让 HTML 到 158KB/233KB；第二篇 5 个 tag、第一篇 4 个；第二篇首版无 updateDate（符合「实质修订才补」的设计，非 bug）。

### 误报（核实过，不用修）

- **「LaTeX 渲染出 bug、源码和渲染结果叠加」**：`.katex-mathml` 是 KaTeX 的无障碍冗余层，CSS `clip-path: inset(50%)` 视觉隐藏，审查者的抓取工具不执行 CSS 才看到双份。真实浏览器无此问题。
- **「活图过誉，只有两处可交互」**：第二篇 6 张、第一篇 9 张活图，hero 文案不算过誉。
- **「折叠框跟普通正文一模一样」**：`.tip` 有 3px accent 左竖线 + 边框 + 背景色，视觉区分明确（同样是无 CSS 抓取的锅）。
- **「post 1 没有最后更新字段」**：post 1 有 `updateDate: 2026-09-10`，锐评看的是旧缓存。

### 09-11 下午补充（用户实测反馈 + 已做的修复）

- **折叠框动作词（已修）**：`.tip-summary` 右端常驻「展开 / 收起」，CSS `::after` 按 `details[open]` 切换文案，label 再长也不影响动作提示。Tip.astro 加 `<span class="tip-toggle">`，样式见 global.css。
- **手机端凯莱图节点过大（已修）**：引擎 `nodeRadius` 缺省 28 是绝对值（viewBox≈容器像素），容器从桌面 ~750 缩到手机 ~354 时节点视觉占比翻倍、盖住边。壳现在按实测宽度（`useSceneState` 的 `viewBoxSize`）线性缩：`w < 640` 时 `nodeRadius = max(14, round(w*0.037))`，与桌面比例一致；3D 同理窄屏给 `nodeScale ?? 0.7`（显式传的 nodeScale 优先）。真机实测 390 视口：2D 节点 28→14，边和箭头全部露出；桌面 1280 保持 28 不变。
- **文章润色（第二篇）**：作者身份大方交代（引擎是自己写的）；A₄ 补直观锚点（正四面体转完与自身重合的 12 种转法）；4.2 十七行瀑布链压成一行链 + 两个顺序例子；4.3 `faceIds` 数组内容省略；4.4「崩」的正文补「崩的是名实不是画面」+ 回指第二节活图；「二十来行/20 行」统一为「三十来行」（按实际代码行数）；一处括注并入正文、一处长段拆分。
- **MDX 坑 +1**：块引用里写 `S*/A*` 会被 Markdown 解析成斜体（两个 `*` 配对），改用下标字符 `Sₙ、Aₙ`。

### 09-11 下午第二批：网站层修复（用户授权「把博客网站本身的问题修了」）

1. **issue 链接插值 bug（真 bug）**：`href="...?title=关于「{post.data.title}」"` 双引号属性内不插值 → 改 `href={模板字符串}` + `encodeURIComponent`。dist 实测两篇链接各自带编码标题。
2. **TOC**：`render(post)` 的 `headings`（MDX 也支持）取 h2+h3，`>3` 条才渲染；宽屏（≥1240px）`position: fixed` 浮在版心右侧（left: calc(50% + 25rem)，版心 46rem 不动），IntersectionObserver 高亮当前节（rootMargin: -70px / -66%）；窄屏 display:none。正文 h2/h3 补 `scroll-margin-top` 防 sticky 头遮挡。
3. **上一篇/下一篇**：文末双卡导航，上一篇 = 更早发布、下一篇 = 更新发布，draft 不参与。窄屏单列。首页仍按日期倒序（进阶篇在前），但导航链把两篇互相接通，新读者能找到入门篇。
4. **代码块复制按钮**：运行时脚本把每个 `pre` 包进 `.pre-wrap`，右上角挂按钮，`navigator.clipboard` 复制 code 文本，成功变「已复制 ✓」1.5s 回弹。桌面 hover 提亮，触屏常显 0.65 透明度。
5. **首页标签真过滤**：span → `button.tag-chip`，`data-tags` 挂在 post-item 上，点击过滤、再点取消、aria-pressed 同步；容器 aria-label 改「按标签筛选文章」；空态提示 `.post-empty`。注意 `.post-item` 有 `display:block`，会顶掉 `[hidden]` 的 UA 样式，必须显式写 `.post-item[hidden]{display:none}`。
6. **页脚**：删「（在线时间 10:00–24:00）」及其 `.foot-note` 样式。

**验证**：build 通过；dist grep 确认 issue 链接各自编码、在线时间为 0；Playwright 桌面 1440（TOC 11 项含 5 个 h3、位置不压正文、滚动高亮工作、复制 10 处全成功且剪贴板内容正确、零页面错误）、390（TOC 隐藏、导航单列）、首页筛选三连点（前端→1 篇 / 取消→2 篇 / 群论→2 篇）全部符合预期。
