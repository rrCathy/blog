# 博客写作手册

从 260909 到现在，每篇复盘沉淀的规则与坑。记的是验证过管用的做法，不是写作理论。已修复问题的完整细节在 git 历史，这里只留仍会再遇到的规则。

---

## 一、发布流程

**一篇新文章就是加一个文件。** `src/content/blog/YYMMDD_短slug.mdx`，commit + push，CI 约两分钟上线。文件名即 URL，短、连字符分词、别用中文。模板在根目录 `post-template.mdx`。

frontmatter 字段（schema 见 `src/content.config.ts`）：

| 字段 | 必填 | 说明 |
|---|---|---|
| `title` | 是 | 显示在列表与浏览器标签 |
| `description` | 否 | 列表页与 RSS 的一句话摘要 |
| `pubDate` | 是 | 发布日期 |
| `updateDate` | 否 | 实质修订时补上，页脚显示「最后更新」 |
| `tags` | 否 | 数组，展示侧自动聚合 |
| `series` / `seriesSlug` | 否 | 同系列每篇必须写同一个 `seriesSlug`，不一致构建期报错；顺序按 `pubDate`，draft 不进系列 |
| `draft` | 否 | `true` 则不进列表与 RSS |
| `cover` | 否 | 封面，写法见下 |

**封面（`cover`）三种写法**（渲染在 `PostCover.astro`）：

```yaml
# 1. 纯图片（public/ 下，路径相对 base）
cover: covers/260917-prisoners.png
# 2. 图片 + alt（alt 用于无障碍与 og:image 说明）
cover:
  img: covers/260917-prisoners.png
  alt: 一句话说明
# 3. 引擎封面：poster 静态海报（engine 配置只用于确定 poster 与 og:image）
cover:
  engine:
    symbol: 'S_{4}'
    view: cayley3d
    actions: '(12),(23),(34)'   # YAML 里必须带引号
  poster: covers/260912-s4.png
```

- 首页横卡与详情页头卡同一版式（文字左、封面右、左缘向卡底色渐隐）；没写 `cover` 则纯文字版式。
- `img` 写法下若存在同名 `-light.png`，自动按主题切换（og:image 恒取深色版）。
- 引擎封面语境由 PostCover 强制（深色、locked、不可交互、无常驻标签），frontmatter 不写；海报与手绘图封面的生成流程见技能 `.workbuddy/skills/blog-cover-poster/`。

**分享**（文末自动启用）：文字复制 + canvas 现画 1200×630 图。两个坑：封面必须传**同源相对路径**（绝对 URL 污染画布，`toBlob` 抛 SecurityError）；分享图恒浅色底。

**MDX 五坑**（都踩过）：

1. 注释用 `{/* */}`，不能用 `<!-- -->`。
2. JSX 属性引号用 ASCII 直引号；**双引号属性值里 `{}` 不插值**，动态属性必须写 `href={模板字符串}`。
3. 表格靠 `remarkGfm`，`astro.config.mjs` 的 `mdx()` 和 `markdown` 两处都要挂；公式走 `remark-math` + `rehype-katex`。
4. 块引用里别写裸 `*`（`S*/A*` 会配对成斜体），用下标字符 Sₙ、Aₙ。
5. **表格单元格里的数学含裸 `|` 会截断整行**：`$|G|$` 的 `|` 被当列分隔符，渲染出来只剩 `$\sum d_i^2 = ` 就没了。改用 `\lvert G \rvert`；行内（非表格）的 `$|G|$` 不受影响。

**写完必验证**：`npm run build` → 去 dist grep 新旧文案（旧 0 处、新 1 处）。同轮连发多个编辑有撞车风险——改完一个文件，确认落盘再改下一个。

---

## 二、内容架构

正文是一条只给初学者的主线（开头放「怎么读这篇」）；严格论证全部收进折叠块 `<Tip label="…">`（纯静态 `<details>`，无 JS）。判断某段该不该下沉只问一句：**不懂这段，主线还能不能读下去？** 能，就下沉。

---

## 三、数学文的两类硬伤

1. **把常见误记当事实。**「奇 = 对换」这类读者最容易犯的错，文章替他们确认反而强化误记。凡要写「显然是」「无非是」的地方，字面算一遍。
2. **隐性预设。**「凯莱图像截角四面体」其实依赖生成元集的选取（同一个 A₄ 换生成元，3-正则变 4-正则）。写定理型结论留个心：这句话对**什么**成立？条件写进正文和表头，别悬着。

核查手段是跑脚本调引擎（`createGroupFromSymbol` 等），不是凭记忆。临时脚本放仓库 `.tmp-*/`，跑完删。

---

## 四、可视化

引擎能画的别手搓 SVG。视图选择：

| 想讲什么 | `view` | 备注 |
|---|---|---|
| 元素构成、摊开看 | `set` | |
| 元素阶、循环子群 | `cycle` | |
| 生成元与连边 | `cayley` | 2D |
| 同上，要立体感 | `cayley3d` | 多面体布局，可拖拽旋转 |
| 运算律、子群、中心 | `table` | 壳已关平移缩放 |
| 几何体的对称作用 | `symmetry` | 配操作台：`picker`、`pickerOrders`、`actionElement`、`autoDemo`，读者自己挑元素看效果 |

- 用颜色图例代替编号；不常驻标签靠 hover（`showLabels={false}`）；子群用 `subgroup` + `members` 直接切表，别在大表里绕。
- **各群 2D 默认形状不同**：A₄/A₅/Sₙ 走 `projection3D`（节点会重叠），S₃ 和循环群走 `circular`（半径与视口无关，高度不够就裁）。查 `getDefaultShape2D(group)`。
- 写「凯莱图依赖生成元」这类主题，活图必须能换生成元；3D 换生成元的对比（节点不动、边全变）远强于 2D。
- 引擎画不了的图（杨图、Kₙ 边分解、映射的有向图）走手绘 Astro 组件，见 `src/components/`；约定：结构色走主题变量、身份色用固定调色板、`compact` 供并排（**并排要包进 `display:flex` 的 div**，组件本身是块级元素）。
- 完整 props 见 `GroupScene.tsx` 的 `GroupSceneProps`。量节点别抓 `<circle>`（坐标在父级 `<g transform>` 上），3D 是 WebGL canvas。
- **配图前先核对三件事（三个坑各中过一个）**：
  1. **组件支不支持你要传的 prop。** `subsets` 只有 `set`/`cycle`/`table` 吃，`CayleyView` 的 props 里**根本没有它**，传了静默无效、图上什么都不发生。以 `node_modules/@groupviz/react/components/Canvas/*.d.ts` 为准，别信印象；想表达「按子群分陪集」在 2D 凯莱图上做不到，换 `view="cycle"`。
  2. **并排要包两层。** 外层 `display:flex` 的 div，**每张图再各自包一层** `flex: '1 1 300px', minWidth: 0` 的 div。`.gv-scene` 的 CSS 写死 `width: 100%`，只做外层 flex、内层不加约束的话，每张图仍会占满整行，看着永远上下堆叠（手绘 `figure` 同理）。
  3. **`layout3D` 预设名望文生义会错。** `hypercube` 给 $C_2^3$ 画出来是团扭曲多面体，要立方体得显式传 `cube`；$C_2^4$ 的缺省布局才是 `hypercube`。**判据永远是真机截图，不是布局名像不像。**

---

## 五、去 AI 味

第一篇扫描出来的指标，写的时候就该盯住：

| 指标 | 初稿 | 定稿 |
|---|---|---|
| 破折号 `——` | 32 | 0 |
| 冒号 `：` | 80 | 0 |
| 双引号 `""` | 55 对 | 7 |
| 加粗片段 | 78 | 26 |
| `恰好`/`正是`/`正好` | 12/10/4 | 1/2/3 |

外加消灭：「不是A而是B」三毒、`值得注意`/`综上所述`/`首先`类套路词、每节反问收尾、升华腔结尾。

**分级原则**：标点禁令按语域降级——清「讲义腔」（“答案是…：”），数学和结构必需的冒号、Tip 属性引号保留；三毒、套路词、加粗密度、升华这些实质项全量照做。

**追加的坑**：

- **最重的 AI 味是「节拍」，不是用词**。FFT 那篇初稿几乎每节收尾都是「本节结论 + 一句加粗对仗金句 + 预告下一节」，五个节尾句式同源；还有「也就是说」「换个说法记住这件事」这类只负责让段落看起来连贯、不推进论证的填充。框架层的重复比零散词汇刺眼得多，改法是整段删、只留实指结论。**能用的自查手法：把每节最后一段和每段最后一句单独抽出来连读**，句式撞车一眼可见。
- **让子代理通读比自查有效**。把上面这份禁用清单交给一个独立 agent 通读全文、按行号列问题句，它会抓出自己写的时候看不见的节拍；**同时要它标出「形式像但必须保留的真锚点」**（指向前文具体数学对象、删掉会断链的承接句，如「第一节的卷积定理还能说具体一点」），避免一味求删。
- **故作短句金句**：两三字断言独立成段，除「显得深刻」外不承担信息量就删。
- **没有上下文的承接句**：「先纠一个命名」这类，指不出前文锚点就重写该段，把「为什么要有这个说法」交代出来。
- **验证清单式罗列**：把检查项加「全部通过」搬进正文是工作记录不是内容——一句话交代「验过、没问题」，细节下沉折叠块。

---

## 六、审稿流程

找真人读，越挑剔越好——自己写的时候看不出硬伤、受众断层和隐藏条件。**修错优先级：事实硬伤 > 结构 > 隐藏条件 > 文字打磨。** 硬伤必须改，不是措辞偏好。

**开头的难度声明要诚实。** 写「记号都随文解释，不需要预备知识」之前先自问：从第几节起会连续引入读者没见过的结构？FFT 那篇的实情是第二节就有商环、理想，第三节是陪集、正合列，「零门槛」是过头话。改法是照实说「这几节放慢一点」，并交代折叠块的用法（深水区在里面，跳过不伤主线）。**把门槛承诺得比实际低，比老实说「要花点力气」更劝退。**

**锐评要核证再动手，四分类**（沿用 §十一）：成立 / 误引 / 部分成立 / 漏项。FFT 第二轮四条里三条完全成立、一条部分成立（作者自己也承认中段松弛有节奏好处）；有一条建议我改了呼应对象——他提的 Rader/Bluestein 跟原论点的数学连接不硬，换成真有连接的（求值点互异 ↔ char 不整除 n）。**不许照单全收，也不许照单驳回。**

改完三项实测，别只信构建：

1. `npm run build` 通过；
2. dist 里 grep 新旧文案（旧 0 处、新 1 处）；
3. 真机截图：本地 chromium + `playwright-core`，390 视口查横向溢出与左右边距（应为 `--gutter`）、公式与折叠块渲染；样式改动用 390/360/768/1280 四挡量对齐。

顺带清理无 JS 环境下会漏渲染的内联公式，能写纯文本就写纯文本。

---

## 七、外部锐评与修复史（索引）

- **2026-09-11 两份锐评**：真 bug（issue 链接插值失效）+ 网站层六项（TOC、上一篇/下一篇、代码块复制按钮、首页标签真过滤、页脚清理）已全部上线；误报四条（`.katex-mathml` 是无障碍冗余层、折叠框有视觉区分等，皆因抓取工具不执行 CSS）。细节见 git 历史。
- **2026-09-18 百囚徒锐评**：刺 1（0.2188）经全枚举证实为误报——「大环 + 尾牌」的组合分拆最易漏类；刺 2（31.2/31.18 混用）属实已统一。见 §十。
- 引擎侧修复史（圆形布局半径、移动端 nodeRadius 自缩、代码块出血与内边距）：细节在 git，验证口径已并入 §六。

---

## 八、一篇里塞多少东西（第五篇）

- **塞太多的症状出现在收尾**：长廊信息密度忽然塌陷——前面每节有例子有展开，最后每条路牌两三句，读者「我看过但不懂」。
- **路牌判据：有没有一个具体到能验证的点。** 只能让人记住一串名词的，不如不讲。
- **重内容三种合法去向：讲透、下沉折叠块、删。** 摆在正文却不给解释是最差的一档。
- **前置承诺**：「第七节会解释」会让读者一直悬着——要么当场铺垫两句，要么不提。
- **讲透的三个做法**：把对象列全（例子少到能列完时全画出来）；给一句递归直觉；说清在哪里停（把没证的式子写出来，明说这里不铺开）——让读者知道坑有多深，比含糊诚实。
- **巧合要消灭，不是点破**：例子和主题撞车时换个例子，往往白捡首尾闭环（绕一圈从另一条路算回 24）。
- **锐评读法**：核事实的判据是「这段是主线的兑现还是旁枝」——兑现就改措辞不改结构；锐评按信息密度判，文章要按读者曲线判。
- **删一节是删一棵依赖树**：指向它的承诺、依赖它的路牌、论证方式都要处理；删完 grep「上一篇」「第 N 节」确认指代还通。
- **图形翻译成文字时易错**：「(4) 是一列」实为一行。描述形状的句子写完对着图再读一遍。
- **折叠块长度的判据不是占比和行数**（占比最高的第三篇效果最好）：看它**在不在当前话题的延长线上**（细节/证明/代码 → 多长都行；另起一个话题 → 一句都嫌多），以及**正文给了多少入口**。单块超 25 行回头看一眼装的是什么。

---

## 九、正文层面的八条（第六篇）

1. **叙事起点必须是真的**：「谁讲的、为什么做、卡在哪」照实写，不许编造动机——事实错了读者发现不了，动机错了作者一眼认出。
2. **定义先现象、后命名、再拆词**：复合术语（完美匹配、无不动点对合）拆成两个词各给一句；措辞逐字检查有没有第二种读法（几何词「相交」在图上常有两种意思）。
3. **概念登场前给「为什么现在问这个」**：补一条「结构量 → 极值问题」的推导链，再命名。
4. **「A 就是 B」必须逐项翻译**：把 A 的每个动作在 B 里的对应物列出来，限制条件一并给——它常比等式本身更有信息量。
5. **图上的例子在正文里走一遍**：关键信息别留在 caption；概念首次出现要有正文级的具体实例。
6. **结构即图形的数据优先画图**：日程表、矩阵、分组默认画图，表格只在需要精确取值时保留；为一张图给组件加输入模式是划算的。
7. **条件式翻译成人话**：要求读者在脑子里做变量代换的表述都是坏表述。
8. **过渡推导用对象本身的迭代**：洗 k 次是 p ↦ 2ᵏp，回原位要 2ᵏ ≡ 1 (mod n−1)——别抽象成「某某的阶」让读者停下来反推。

---

## 十、数字被质疑时的核法（第七篇）

- **数字自带计数来源**（`8820/40320` 这种形式），让谁想复核都有落点。
- **被质疑先全枚举**，再对照对方的枚举漏了哪几类。**「大环 + 尾牌」的组合分拆最易漏类**：n=8 含 4-环有五组类型（4+1⁴、4+2+2、4+4、4+2+1+1、4+3+1），漏两组误差三倍。
- **主数值五处同口径**（frontmatter、caption、图内标注、aria、正文）；并列列举的序列可整体用约值，但别混进主数值的精确写法。

---

## 十一、算法技术文（第九篇，Todd-Coxeter）

面向「把一个算法讲透」的写作（手算到代码、证明、外延），与数学长文同层级，但验收标准多一条：**读者的手能跟着走**。

1. **术语消歧先于动笔。** 一个词承担两个语义就是事故：初稿「行 1」既是表格物理行又是陪集编号，每读一处都要消歧。改名字制（$H$、$Ha$、$Hab$……，名字 = 从 $H$ 走到它的词）后三重红利：格子自解释（$(Hab,a)=(Hab,b)=Haba$ 的平行肉眼可见）、名字即定义词（证明里「定义词 $w_i$」整段术语消失）、碰撞语义变成「两个名字指同一份」。**定稿前做记号审查：每个符号的每种读法列出来看。**
2. **例子驱动不是「例子开头」，是算完才许出现第一个定义。** 第二节两版失败：先规定后验证（无锚点）→ 终表先行但立刻抽象化（仍是颁布）。定稿：摆元素 → 算切份 → 填格 → 读表 → 归纳规则与动作 → 从空表重走验证。
3. **承诺与兑现同节。** 表一闭合，读者最想知道「这是谁」；把它挪到第五节就成了「看完第三节看第四节突兀」。改回第三节末（表闭合即揭晓），第五节只剩证明。
4. **叙述与脚本事件序列逐步对齐；脚本行为决定叙述细度。** two-pointer 倒走决定 $(Haba,b)=Ha$，热身就必须讲它（白话一句），不能因为嫌啰嗦砍掉——砍了与第三节的数字矛盾。
5. **教学叙述选「更聪明的一版」要显式披露实现差异。** $Habaa$ 尾部 $aa$ 撞 $a^2=e$ 直接约化成 $Hab$（正文按约化讲，A₄ 只剩一次碰撞），随文代码不做约化、多绕一步——正文加诚实注记，否则读者拿代码复算对不上。
6. **MDX 里的代码块 = 运行过的代码。** 写完从 MDX 抽出重跑（本次真抓到 `gens` 传字符串导致 `str+list`）；**改注释也要重跑**。
7. **窄屏表格：长文本列必溢出。** 站点约定窄屏 `table` 是 `nowrap` + 横滚，实测三列中文长文本表在 390 视口溢出 205px。表格内长文本压成速记（「补 $n$／并 $n$」），术语在表前一句交代，别指望换行。
8. **锐评核证三分类：成立 / 误引 / 漏项。** 本篇七条成立、一条误引（「第三节预告 A₅」实为 A₄ 的预告）、一条漏项（「两次塌方」过时，自查抓到）。**事实硬伤先修**（错的注释规则、过时表述），密度建议次之。
9. **配引擎图前先问「这个形状的顶点表按哪套生成元硬编码」。** A₄ 截角四面体按缺省 $(12)(34),(234)$ 硬编码，传 $(123)$ 图就烂。涉及具体实现的配图统一用缺省对（正文抽象记号不受影响）；**海报同病，配图改了海报必须重截**。
10. **史实以检索为准，轶事有源才用。** 论文题名差点写错（实为 A practical *method* for enumerating cosets…）；「Todd 的生日礼物」查无实据直接弃用，改用有源的墙纸细节（旧墙纸背面演算，一小时约 200 个陪集）。

---

## 十二、算法技术文（FFT）：讲清楚与高观点怎么共存

与 §十一 同层级，都是把算法讲透。这篇多出来的要求是**「不仅要讲清楚，还要把高观点讲明白」，两条都要**，于是长出一批新坑。

1. **参考材料不是免检品，公式要跑验算。** 素材里的分解式按 $k = k_1+n_1k_2$ 展开会漏掉 $\omega^{n_1^2 j_2 k_2}$ 一项，一般不成立；换成 $j = j_1+n_1j_2$、$k = k_2+n_2k_1$ 之后，内层正好落在子群陪集上、外层落在商群上。**五组参数（$(n_1,n_2)$ = (3,4)/(4,3)/(2,4)/(6,2)/(4,4)）数值对照误差 ~1e-14** 才敢落笔。
2. **高观点要落到能算实的对象上，不是形容词。** 全篇最有力的一句是「旋转因子 = 短正合列不分裂的度量」：可验算（截面偏差落在 $H$ 里）、有判据（分裂 $\iff \gcd(n_1,n_2)=1$）、能推出算法差异（分裂时旋转因子整个消失）。**判据：这个高观点能不能推出一个具体的算法差异，推不出就是形容词。**
3. **第一次实质用到某个经典算法时就把名字挂上。** 第三节讲的就是 Cooley-Tukey，但初稿到第六节才出现这个词，读者会问「他是谁」。当场点名即可，顺带一句史实（Gauss 1805 用过未发表；$n=2^m$ 叫 radix-2，一般因子分解叫混合基）。
4. **最锋利的那句话放在小节第一句。** 「旋转因子 = 不分裂的度量」初稿藏在推导末尾，被锐评指出「重点淹没」。改成小节开头先亮论点、推导收尾用「开头那句话到这里算实了」回扣。
5. **图上的零件第一次出现要解释名字来历。**「蝶形」用了半篇，读者不知道指什么。补「上下两根输入线中间交叉、像一对翅膀」一句就够。
6. **手算不是安慰性内容，接回机制才算数。** 4 点手算走完补一句：$n_1=n_2=2$、$\gcd=2$、正合列不分裂，所以合并那一步真的出现非平凡的 $-i$。接上之后中段的松弛保住了，「孤立小练习」的观感也消失。
7. **每个「显然」的等式都问一句读者跟不跟得上。** $\omega^{n_2j_1k_1}=\omega_{n_1}^{j_1k_1}$ 缺了中间步 $\omega^{n_2}=e^{2\pi i n_2/n}=e^{2\pi i/n_1}=\omega_{n_1}$，被锐评当场逮住。凡是跨了一步周期换算的等式，把代换写出来。
8. **「记账规则」型结论要用具体下标走一遍。** 位反转抽象地说「从根到叶的路径位序相反」读者会跟丢；改成拿 $j=1$ 走三层（奇、偶、偶 $\to 100$，而 $1$ 是 $001$），后面再挂一张八项对照表。
9. **DIT 与 DIF 的层序容易记反，写前先手推。** DIT：输入位反转、层内跨距 $1\to2\to4$（递增）、输出自然序；DIF：输入自然序、跨距 $4\to2\to1$、输出位反转。两者拓扑相同，旋转因子落在不同的层上。
10. **抽象记号的入门门槛靠拆。** $\mathbb{C}[G]$ 初稿直接给集合构造式 $\{\sum a_g e_g : a_g \in \mathbb{C}\}$，被锐评点名「术语陌生」；改成先中文描述（给每个群元素配一个基向量、配一个复系数、加起来）再给式子，同一条定义的读入门槛差很远。

---

## 十三、工具性概念的次序（第十一篇，Sₙ 三张凯莱图）

前两轮修的是「动机」，这一轮修的是**次序**：概念本身的动机都交代了，但它们登场的先后错了，读者会被迫用后面的知识读前面。

1. **工具性概念的建立顺序 = 使用顺序，不许抢跑。** 本篇的链条是**取对偶 → 换成平面上的格 → 才有基本域**。初稿直接从环面嵌入跳到「基本域」，对偶要到两节之后才讲。判据：正文里**第一次使用**某概念的位置之前，必须有一段专门交代「为什么需要它、它是什么」，而且那段不能依赖任何后文。三个概念就是三处硬检查点。
2. **反例放在分类结果之后。** 那张矩形周期的图之所以构成反例，是因为分类给出了 8 类、它落在第 7 类。放在分类之前讲，读者手里没有参照系，还会提前引用类号。**顺序：先做出分类，再回到具体的那一类。** 同样，正文里出现「第 N 类」之前，8 类的表和图必须已经就位——不然就是让读者翻页对答案。
3. **图注与正文的用词必须与图里真正画的对象逐字对上。** 配图里画的是「12 个面心（三角格格点）」，正文写成「12 个六边形落在一块平行四边形的区域里」，这就是错。配图完成后做一遍**指称核对**：把图里每个可见元素点名，再逐处检查正文提到它时用的是不是同一个词。同一张图换个语境复用（8 类网格里的一格 → 单格放大）时，caption 要重写，别沿用旧句。
4. **结构改完做一遍跨节引用扫尾。** grep 的清单：`第[一二三四五六七八九十]节`、`值得`、`换句话说`、`也就是说`、`恰好|正好|正是`、以及被删小节的标题词。本次扫出 7 处待改（一处节号错位、四处 AI 味词、两处抢跑）。
5. **参考资料里的可视化 HTML 是资产，可以本地跑通再截图。** 依赖 CDN 时改挂本地 `three@0.128.0`（走 npm registry），去掉 UI 与 autoRotate，相机参数化，页面内做数据自校验（顶点/边/面计数），再截图。比用 Python 复刻投影省事，且与参考逐字一致。**用 `require` 的脚本存 `.cjs`。**
6. **自己即兴编的解释和引用的材料一样要验算。** Burnside 折叠块里我临时编的 $(\mathbb{Z}/12)^\times$ 例子（"轨道数除以 5"）是错的，返工时整段删掉，换成只解读已验证的公式。编不出一个能跑验算的解释就别硬编，直说「这条只给结论、证明不在此展开」，比塞一个错的「为什么」诚实得多。临时编的例子是审稿盲区：它是自己写的，通读时不会怀疑。**举例时逐个数字都要跑**：$S_5$ 的 $(2345)$ 被当成"含 1 的四轮换"举出来（实为固定 1，距离 5 而非 3），一句话错在两层——例子错，且暴露了"含 1／不含 1"在循环记号下的歧义（读者看 $(2345)$ 字面找不到 1）。改法是用 $(1234)$ 与 $(2345)$ 做一对最小对照，把判据的口径当场写死。
7. **锐评是分层的，一轮只揭一层。** 本篇四轮锐评的病根依次是：内容硬伤（数字）→ 内容缺失（该讲的没讲）→ 动机缺失（不讲为什么）→ 次序错误（先用后定义）。修完一层，下一轮才暴露下一层，别指望一轮修完。**而且结构大改本身会引入新的次序问题**——第三轮重排时我把基本域挪进第四节、对偶留在第六节，恰好制造了第四轮抓到的错位。每次大改后重走第 1 条的检查。

